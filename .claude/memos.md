# Memos

Off-task ideas captured during sessions, to revisit later. Managed by `/memo`; open items resurface at session start, task completion, and commit.

- [x] 2026-08-30 21:21 — extend documentation checking skill to update screenshots
- [ ] 2026-08-31 20:42 — Migrate GitHub Pages from the legacy branch build to a workflow-based deploy (`actions/configure-pages` + `upload-pages-artifact` + `deploy-pages`, with a committed `pages.yml` and Gemfile per repo). All seven Pages sites are currently `build_type: legacy` — achievement-overlay, bga-assistant, chrome-assistant, jsonl-logs-intellij-plugin, url-cleaner, claude-code-dashboard, ai-agent-dashboard-electron — which pins them to whatever the `github-pages` gem ships (v232 / Jekyll 3.10.0, verified from a build log 2026-08-31) rather than a Jekyll version they choose. The concrete pain that prompted this: a failed legacy build keeps serving the previous deploy while `/pages/builds/latest` sits on `building`, so a broken site reads as a change that did nothing — the failure was only visible via `gh run list`. Migrating also means the `github-pages` skill's publish step stops being two `gh api` calls and starts committing a workflow, so update the skill in the same pass. Do it as a deliberate migration across all seven, not as a side effect of touching one.
