---
name: User GitHub account
description: User's GitHub username is AnotherSava — use this to distinguish their own repos from third-party clones
type: user
---

The user's GitHub account is **`AnotherSava`**.

When asked about "my repos" / "my projects" / similar, filter by `origin` URL pointing at `github.com:AnotherSava/*` (or `https://github.com/AnotherSava/*`). Treat any repo whose `origin` belongs to a different account as a third-party clone, even if it lives under `D:\projects\`.

For forks the user maintains, `origin` is theirs and `upstream` points at the original author — both are still "their" repos.
