---
name: GitHub identity
description: User's GitHub username is AnotherSava; distinct from their git author name Oleg Savelyev
type: user
---

GitHub username: **AnotherSava**.

Distinct from the git author name `Oleg Savelyev` that appears in commits. When constructing repo URLs, `gh` API calls, or GitHub noreply email patterns (`<user-id>+AnotherSava@users.noreply.github.com`), use the login `AnotherSava`, not the author name.

Public repos live under `github.com/AnotherSava/`. GitHub Pages under `anothersava.github.io/<repo>/`.
