# Atlas

A small static site that draws the places a trip touched onto a map. Built from the
shape of a public project in the fleet that carries no LICENSE: a README, a web
directory holding the app, and a manifest under it.

Open `web/index.html` to run it. There is no build step.
