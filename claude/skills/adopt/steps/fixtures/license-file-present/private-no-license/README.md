# Ledger

An internal tool nobody outside the team has a copy of. Built from the shape of the two
private repos in the fleet that carry no LICENSE: a README, a web directory holding the
app, and a manifest under it that still names a license the repo root does not.

Run `npm --prefix web run check` before committing.
