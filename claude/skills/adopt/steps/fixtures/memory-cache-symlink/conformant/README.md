# Fixture repo

A repo that commits its project memory. Its machine-side cache is wired by the step before
verify is asserted: the link is named after wherever this tree sits, so no committed tree
can carry it.
