# npm treats `engines` as advisory by default - it warns and installs anyway, so the range in
# package.json, the .nvmrc beside it and the base image can all agree while someone quietly builds
# on another runtime. This turns that silent mis-install into EBADENGINE.
engine-strict=true
