# A repo with a Node project and no npm version anywhere

Nothing here names a `packageManager`, and nothing names one to copy. Which version to pin is a
choice about every machine that installs this project, so the step stops and asks rather than
writing a number it picked. `apply` exits 3 and leaves this manifest exactly as it is.
