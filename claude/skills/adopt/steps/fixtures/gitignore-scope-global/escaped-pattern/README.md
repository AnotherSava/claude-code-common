# escaped-pattern

The backslash keeps a trailing space that `.strip()` eats, so the text this step would compare
against the global excludes file is not the text anyone wrote. Apply stops, names the line, and
leaves the `.DS_Store` duplicate beside it in place — a deletion decided on a reading that may be
wrong is worse than one nobody made.
